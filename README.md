# tui-theme

터미널에서 이미지를 1-bit 픽셀아트 스타일로 프리뷰하는 TUI 앱.

## 빌드 & 실행

```bash
go build -o .tui .
```

### 이미지 설정 (인터랙티브 TUI)

```bash
./.tui
```

이미지 경로를 입력하면 흑백 픽셀아트 프리뷰를 확인할 수 있습니다.

### 설정된 이미지 출력

```bash
./.tui show
```
